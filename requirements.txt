# Core dependencies. Install with:  pip install -r requirements.txt
requests>=2.31.0        # fetching web pages
beautifulsoup4>=4.12.0  # reading the HTML that comes back
openai>=1.40.0          # talking to the OpenAI API
python-dotenv>=1.0.0    # loading your key from .env

# Development only
pytest>=8.0.0           # running the tests
